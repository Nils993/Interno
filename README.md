# Установка и запуск проекта

## 1. Установите зависимости

Сначала откройте терминал и выполните команду:

`npm install `

## 2. Запустите проект в режиме разработки

После установки зависимостей, запустите проект с помощью команды:
`npm run serve`
Теперь вы сможете открыть проект в браузере по адресу `http://localhost:8080` (или другому, указанному в терминале).

## 3. Подготовка к продакшену

Если вы хотите собрать проект для продакшена, выполните команду:
`npm run build`
Это создаст оптимизированную версию вашего приложения.

## 4. Проверка кода на ошибки

Чтобы проверить код на наличие ошибок и автоматически исправить их, используйте команду:
`npm run lint`

## Дополнительная информация

Для настройки конфигурации проекта вы можете ознакомиться с [документацией](https://cli.vuejs.org/config/).
